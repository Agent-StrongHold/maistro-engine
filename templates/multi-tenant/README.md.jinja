# {{ project_slug }}

Multi-tenant product seed from **maistro-engine** template `multi-tenant`.

- Substrate: `{{ substrate_ref }}`
- Stronghold-specific isolation and policy engines belong in this product repo, not in `maistro-engine`.
