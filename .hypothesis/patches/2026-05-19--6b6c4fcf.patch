From HEAD Mon Sep 17 00:00:00 2001
From: Hypothesis 6.151.10 <no-reply@hypothesis.works>
Date: Tue, 19 May 2026 19:29:31
Subject: [PATCH] Hypothesis: add explicit examples

---
--- ./formal/models/test_rate_limiter.py
+++ ./formal/models/test_rate_limiter.py
@@ -56,6 +56,11 @@
     n=st.integers(min_value=0, max_value=200),
 )
 @settings(max_examples=60)
+@example(
+    rpm=2,
+    burst=1,
+    n=1,
+).via('discovered failure')
 async def test_rpm_enforcement(rpm, burst, n):
     config = RateLimitConfig(requests_per_minute=rpm, burst_limit=burst, enabled=True)
     limiter = InMemoryRateLimiter(config=config)
