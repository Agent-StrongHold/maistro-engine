From HEAD Mon Sep 17 00:00:00 2001
From: Hypothesis 6.151.10 <no-reply@hypothesis.works>
Date: Tue, 19 May 2026 19:27:40
Subject: [PATCH] Hypothesis: add explicit examples

---
--- ./formal/models/test_rate_limiter.py
+++ ./formal/models/test_rate_limiter.py
@@ -93,6 +93,11 @@
     rpm=st.integers(min_value=5, max_value=20),
 )
 @settings(max_examples=50)
+@example(
+    key_a='0',
+    key_b='0',
+    rpm=5,  # or any other generated value
+).via('discovered failure')
 async def test_independent_keys(key_a, key_b, rpm):
     config = RateLimitConfig(requests_per_minute=rpm, burst_limit=rpm, enabled=True)
     limiter = InMemoryRateLimiter(config=config)
