# Conductor (standalone) — frozen snapshot

**`snapshot/`** is a copy of `/Users/blake.matthews.-nd/Conductor` without `.venv`, `.git`, caches (see `snapshot/PROVENANCE.txt`).

Python `gateway/` (Ultra Think, slot manager, prefix cache) and `orchestrator/`. Distinct from the larger `conductor-orchestrator/` tree under `potential-dead-code/code-worth-implementing-from-Project-mAIstro/`.
