# Complete legacy maistro site (copy)

**Retention:** [`docs/specs/SPEC-178-legacy-snapshot-retention.md`](../../docs/specs/SPEC-178-legacy-snapshot-retention.md).

This directory was a **`cp -R` snapshot** of `potential-dead-code/legacy-maistro-site/` at one point in time. The **authoritative porting spec** for the hyperagent slice is [SPEC-177](../../docs/specs/SPEC-177-hyperagent-graph-execution.md).

When SPEC-177 is implemented and the team no longer needs a second full-tree copy, delete **this** duplicate first (or together with `potential-dead-code/legacy-maistro-site/` per SPEC-178).
