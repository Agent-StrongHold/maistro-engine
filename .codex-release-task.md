Temporary task marker. Remove this file before merge.

Address the remaining Codex findings from PR #371 on this branch:
1. README release inventory must not claim deleted Astro frontend or Flutter README shell exist. Update those rows to accurately describe current tree or remove them.
2. `.github/workflows/release-installer.yml` must only publish production/latest images from refs proven to be on reviewed `main` history, including workflow_dispatch and v* tags.
3. Image publication must occur only after the installer smoke test succeeds. Do not push/overwrite `:latest` before smoke validation.
4. Add or update focused validation/tests or workflow-lint-compatible guards as appropriate.
Keep the patch narrow and delete this marker before merge.
