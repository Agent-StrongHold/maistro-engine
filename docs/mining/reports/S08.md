# S08 Mining Report: Episodic Memory, Scopes, Outcomes, Decay/Consolidation

**Slice:** S08 (product-agnostic memory subsystem)  
**Source:** /home/user/stronghold/src/stronghold/memory/ (episodic, scopes, outcomes, mutations; excludes learnings/)  
**Target:** /home/user/maistro-engine/packages/maistro-core/src/maistro/memory/  
**Start date:** 2026-07-04 UTC  

---

### /home/user/stronghold/src/stronghold/memory/__init__.py:1-2

- What: Package docstring — minimal, describes subsystem scope
- Engine coverage checked → present
- Gap severity: none
- Action: no action needed
- Effort: N/A
- Product-agnostic?: yes

---

### /home/user/stronghold/src/stronghold/memory/episodic/tiers.py:1-66

- What: Weight clamping (clamp_weight), reinforce/decay functions with tier-based bounds
- Engine coverage checked → partial
- Gap severity: **HIGH**
- Action: Maistro has 4x more tiering logic — `on_access()` (refresh last_accessed_at), `reclassify()` (promote WISDOM >= WISDOM_PROMOTE_THRESHOLD, demote REGRET >= REGRET_DEMOTE_THRESHOLD), `on_feedback()` (boost/slow/drop/speed with decay_rate scaling, then reclassify), `tick_decay()` (time-based decay scaled by elapsed hours since last_access). Stronghold only has basic reinforce/decay with static weight clamping. Missing constants: WISDOM_PROMOTE_THRESHOLD, REGRET_DEMOTE_THRESHOLD, BOOST_RATE, DROP_RATE, SLOW_DECAY, FAST_DECAY, EpisodicMemory.decay_rate field.
- Effort: **high** — requires adding decay_rate field to EpisodicMemory type, thresholds to types.memory, and 4 new functions in tiers.py
- Product-agnostic?: yes

---

### /home/user/stronghold/src/stronghold/memory/episodic/__init__.py:1

- What: Package docstring
- Engine coverage checked → present
- Gap severity: none
- Action: no action needed
- Effort: N/A
- Product-agnostic?: yes

---

### /home/user/stronghold/src/stronghold/memory/scopes.py:1-39

- What: Memory scope filtering: `build_scope_filter()` — hierarchical scope chain (GLOBAL > ORG > TEAM > USER > AGENT > SESSION)
- Engine coverage checked → partial
- Gap severity: **MEDIUM**
- Action: Maistro adds `matches_scope()` function (lines 27-57 in maistro scopes.py) that separates scope matching logic from store. Stronghold embeds logic in both scopes.py and episodic/store.py. Maistro's approach is cleaner: scopes.py handles filter construction and matching, store just calls matches_scope(). Strongly recommend porting maistro's `matches_scope()` back into stronghold (or vice versa if stronghold preferred its inlining).
- Effort: **low** — refactor to use common matches_scope function
- Product-agnostic?: yes

---

### /home/user/stronghold/src/stronghold/memory/episodic/store.py:1-110

- What: InMemoryEpisodicStore with scope-filtered retrieve and reinforce
- Engine coverage checked → partial
- Gap severity: **MEDIUM**
- Action: Maistro has additional `list_by_scope()` method (lines 56-83 in maistro episodic/store.py) supporting project_id, min_weight filtering, and scope-independent project lookup. Stronghold missing project_id support. Also, stronghold has inline _matches_scope (duplicating code from episodic/store.py line 9-45); maistro imports matches_scope from scopes module (cleaner DI).
- Effort: **medium** — add list_by_scope() method, pull project_id from EpisodicMemory type, refactor scope logic to use shared matches_scope()
- Product-agnostic?: yes (project_id is soft scope per ADR-019/035)

---

### /home/user/stronghold/src/stronghold/memory/episodic/retrieval.py:1-117

- What: ScoredEpisodicRetrieval with optional embedding reranking
- Engine coverage checked → partial
- Gap severity: **MEDIUM**
- Action: Scoring formula differs. Stronghold: `score = sim * weight` where sim is either lexical OR vector (fallback). Maistro (line 88): `score = (lexical + vector) * weight` — combines both signals before scaling. Maistro's approach is more robust: lexical and vector are complementary (different relevance signals), not alternatives. Stronghold's fallback structure loses information. Also, maistro imports matches_scope from scopes module; stronghold imports _matches_scope from store module (circular-ish pattern).
- Effort: **medium** — change scoring formula to combine lexical+vector, refactor scope logic
- Product-agnostic?: yes

---

### /home/user/stronghold/src/stronghold/memory/outcomes.py:1-224

- What: InMemoryOutcomeStore with task completion rates, experience context, usage breakdown, daily timeseries
- Engine coverage checked → partial
- Gap severity: **MEDIUM**
- Action: Maistro adds two helper functions: `_format_failure_lines()` and `_format_thumb_lines()` (lines 17-37 in maistro outcomes.py) for cleaner markdown rendering. Maistro's `get_experience_context()` (lines 94-141) has project_id parameter and surfaces both hard failures and user thumbs-down outcomes; stronghold only has failures. Maistro's _org_matches (lines 250-253) allows system callers (caller_org="") to see all outcomes; stronghold's (lines 218-223) restricts system callers to unscoped records only. Maistro's approach is more flexible for dashboards (allow system LLM to see failures across orgs).
- Effort: **medium** — add helper functions, refactor get_experience_context() for thumbs-down, add project_id parameter, relax org_matches for system callers
- Product-agnostic?: yes (thumbs-down is general feedback mechanism)

---

### /home/user/stronghold/src/stronghold/memory/mutations.py:1-32

- What: InMemorySkillMutationStore with record and list_mutations
- Engine coverage checked → present
- Gap severity: none
- Action: Maistro has identical implementation; no changes needed
- Effort: N/A
- Product-agnostic?: yes

---

### /home/user/stronghold/src/stronghold/memory/episodic/consolidation.py

- What: NOT PRESENT IN STRONGHOLD
- Engine coverage checked → missing
- Gap severity: **CRITICAL**
- Action: Maistro has full consolidation subsystem (145 lines in maistro/memory/episodic/consolidation.py) implementing ADR-080 part B (SPEC-241): merge similar memories, flag contradictions, weighted average merge weight, batch consolidation. Core functions: `consolidate()` (propose merges/contradictions), `apply_merge()` (update primary, mark absorbed deleted), `apply_contradiction()` (lower both sides' weight, flag for review), `run_batch()` (apply proposals to store). Stronghold has ZERO consolidation logic. This is episodic memory hygiene — prevents duplicate/contradictory memories from accumulating.
- Effort: **high** — implement full consolidation module with similarity_fn/contradiction_fn callbacks, dataclasses, batch reporting
- Product-agnostic?: yes

---

## Summary

**Files analyzed:** 8 (episodic/tiers, episodic/__init__, scopes, episodic/store, episodic/retrieval, outcomes, mutations, missing episodic/consolidation)

**Gap distribution:**
- Critical: 1 (consolidation missing)
- High: 1 (tier decay/reinforcement dynamics)
- Medium: 4 (scopes refactoring, store list_by_scope, retrieval scoring, outcomes thumbs-down)
- None: 2

**Key missing product-agnostic capabilities:**
1. Consolidation (merge + contradiction detection) — hygiene for growing memory stores
2. Decay/reinforcement tier transitions (WISDOM promotion, REGRET demotion) — memory self-improvement
3. Time-based decay (tick_decay with elapsed hours) — prevents stale memories
4. Project-scoped memory retrieval — cross-scope lookup for project context
5. User feedback integration (thumbs-down) — outcome-driven memory refinement
6. Hybrid scoring (lexical + vector) — better relevance ranking

**Stronghold ownership tags:** None — all analyzed files are product-agnostic per ADR-019/035.

---

**Next phase:** Port consolidation module, add decay_rate field, refactor scope logic to shared functions, enhance scoring and outcome feedback.
