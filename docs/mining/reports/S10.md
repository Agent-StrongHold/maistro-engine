# S10 Mining Report: Stronghold Orchestrator → Maistro Engine

**Source:** `/home/user/stronghold/src/stronghold/orchestrator/`
**Target:** `/home/user/maistro-engine/packages/maistro-core/src/maistro/orchestrator/`
**Scope:** Work item queuing, super-planner decomposition, master-orchestrator execution, worker lifecycle, retry/lease logic.

---

## Findings

### orchestrator/__init__.py:1-7
- What: Module docstring (organization/trigger dispatch/work tracking/events)
- Engine coverage checked: `__init__.py` exports 17 classes from hierarchy, master, planner, waves → present
- Gap severity: missing
- Action: skip (docstring only, no executable code)
- Effort: S
- Product-agnostic?: yes

### orchestrator/engine.py:27-33 (WorkStatus enum)
- What: Enum for work item lifecycle: QUEUED → RUNNING → COMPLETED | FAILED | CANCELLED
- Engine coverage checked: master.py has `WorkItemStatus` with PENDING, IN_PROGRESS, PASSED, FAILED, BLOCKED, SKIPPED → partial
- Gap severity: partial
- Action: port
- Effort: S
- Product-agnostic?: yes

### orchestrator/engine.py:35-68 (WorkItem dataclass)
- What: Work item model with id, agent_name, messages, trigger source, priority tier, metadata, status tracking, timestamps, log
- Engine coverage checked: master.py `WorkItem` has: id, group, task_id, description, agent_role, depends_on, status, result, timestamps, metadata → partial
- Gap severity: partial
- Action: merge (Stronghold's trigger/messages/priority fields missing; engine's group/depends_on present)
- Effort: M
- Product-agnostic?: yes

### orchestrator/engine.py:74-283 (OrchestratorEngine class)
- What: Async queue-based work dispatcher with priority tiers, worker pool, concurrent execution, work-item lifecycle, event emission
- Engine coverage checked: master.py `MasterOrchestrator` does wave-based execution with dependency checking + retries + security gates → different architecture
- Gap severity: partial (different paradigm: queue-based vs. wave-based)
- Action: merge as new construct (Stronghold's simple queue model complements engine's wave orchestration)
- Effort: L
- Product-agnostic?: yes

### orchestrator/engine.py:99-108 (start/stop lifecycle)
- What: Async lifecycle methods to start worker pool and graceful shutdown with task cancellation
- Engine coverage checked: MasterOrchestrator has `execute()` but no background worker pool or lifecycle control → missing
- Gap severity: missing
- Action: new method (engine uses synchronous execute, Stronghold has async worker loop)
- Effort: M
- Product-agnostic?: yes

### orchestrator/engine.py:117-148 (dispatch method)
- What: Submit work item to priority queue, create WorkItem, append to queue with tier-mapped priority
- Engine coverage checked: MasterOrchestrator.load_plan() accepts waves but no runtime dispatch API → missing
- Gap severity: missing
- Action: port (adds dynamic work submission outside of static plan)
- Effort: M
- Product-agnostic?: yes

### orchestrator/engine.py:150-176 (get, list_items, cancel, status methods)
- What: Query API: get single item, list all (filterable by status), cancel queued items, fetch engine status summary
- Engine coverage checked: MasterOrchestrator.get_progress() exists but no per-item get(), no list filtering, no cancel → partial
- Gap severity: missing
- Action: port
- Effort: M
- Product-agnostic?: yes

### orchestrator/engine.py:177-237 (_worker method)
- What: Worker coroutine loop (queue consumer): dequeues, marks running, executes via _execute(), tracks timestamps, emits completion/failure events
- Engine coverage checked: MasterOrchestrator._execute_wave() uses Semaphore for concurrency but no worker loop; waves are synchronously awaited → different
- Gap severity: partial (different execution model: background workers vs. wave loop)
- Action: merge (background workers provide independent progress; engine's wave loop is plan-driven)
- Effort: L
- Product-agnostic?: yes

### orchestrator/engine.py:239-272 (_execute method)
- What: Execute single work item: lookup agent, call agent.handle() with system auth, return response with tool_history
- Engine coverage checked: MasterOrchestrator._execute_item() calls handler (registered per role), updates status → partial
- Gap severity: partial
- Action: port (handler dispatch pattern + agent execution)
- Effort: M
- Product-agnostic?: yes

### orchestrator/engine.py:274-282 (_emit_event method)
- What: Emit event to reactor.emit(Event) for downstream processing (learnings, audit, notifications)
- Engine coverage checked: no event emission in master/planner; events are in maistro.events module → missing
- Gap severity: missing
- Action: port (event-driven work completion tracking)
- Effort: M
- Product-agnostic?: Stronghold-owned (skip — reactor is Stronghold-specific concept; engine has generic event bus)

### orchestrator/pipeline.py:1-47 (build_spec_summary, _emit_and_save_spec, _enrich_spec_with_property_tests)
- What: Spec emission and enrichment utilities: summarize spec, emit from issue metadata, generate property tests from invariants
- Engine coverage checked: builders/spec_emitter.py exists in engine → partial (engine has spec infrastructure but orchestration hooks different)
- Gap severity: partial
- Action: port (spec-driven stage conditioning is builders-specific; extract spec-summary pattern for general use)
- Effort: M
- Product-agnostic?: partial (spec enrichment is product-agnostic; emit_spec integration is builders-specific)

### orchestrator/pipeline.py:77-108 (StageStatus enum, PipelineStage dataclass)
- What: Stage lifecycle (PENDING → RUNNING → COMPLETED | FAILED | SKIPPED) and stage model with skip_if conditions
- Engine coverage checked: no stage-level enums/models in engine; only WorkItem status → missing
- Gap severity: missing
- Action: port (pipeline stages are a higher-level choreography abstraction; not present in engine)
- Effort: S
- Product-agnostic?: yes

### orchestrator/pipeline.py:110-135 (PipelineRun dataclass)
- What: Pipeline execution tracker: id, issue_number, title, repo, stages list, current_stage index, status, context dict
- Engine coverage checked: no pipeline-run concept; MasterOrchestrator tracks waves but not multi-stage pipelines → missing
- Gap severity: missing
- Action: port (pipeline aggregates work items into ordered stages; new abstraction)
- Effort: M
- Product-agnostic?: partial (issue_number/repo are GitHub-specific; generic pipeline structure is product-agnostic)

### orchestrator/pipeline.py:139-221 (BUILDER_PIPELINE constant)
- What: Hardcoded 5-stage builder pipeline: decompose (quartermaster) → scaffold (archie) → implement (mason) → review (auditor) → cleanup (gatekeeper)
- Engine coverage checked: builders/orchestrator.py exists; no hardcoded pipeline stages, modular design → different
- Gap severity: missing (as constant; engine favors configuration)
- Action: port as config (move to config/builder_stages.yaml or builders.yml per ADR-068)
- Effort: M
- Product-agnostic?: Stronghold-owned (skip — builder pipeline is specific to code generation; engine's general planner handles all tasks)

### orchestrator/pipeline.py:224-441 (BuilderPipeline class)
- What: Orchestrate multi-stage issue-to-merge pipeline with spec-driven verification, exponential-backoff polling, timeout/cancel handling, stage skipping
- Engine coverage checked: MasterOrchestrator + SuperPlanner exist but no stage choreography, polling loop, or spec verification gates → missing
- Gap severity: missing (completely different execution model)
- Action: port as new construct (pipeline orchestration is distinct from wave orchestration; two patterns needed)
- Effort: L
- Product-agnostic?: Stronghold-owned (skip — this is builders/code-pipeline specific; engine has general SuperPlanner/MasterOrchestrator for any goal)

### orchestrator/pipeline.py:249-348 (execute method: stage loop setup)
- What: Initialize run, load spec, build context, loop over stages with spec summary injection
- Engine coverage checked: MasterOrchestrator.execute() loops over waves; no spec context passing, no stage skipping logic → partial
- Gap severity: partial (wave loop exists; stage conditioning/context building different)
- Action: merge (extract conditional stage-skip pattern for general reuse)
- Effort: M
- Product-agnostic?: yes

### orchestrator/pipeline.py:350-391 (execute method: dispatch, poll, error handling)
- What: Dispatch work via engine.dispatch(), poll with exponential backoff (1s → 10s), handle timeouts/failures, extract output
- Engine coverage checked: MasterOrchestrator.execute() awaits waves directly; no polling, no independent timeouts per stage → missing
- Gap severity: missing (polling is specific to orchestrating independent engine work items)
- Effort: L
- Product-agnostic?: yes

### orchestrator/pipeline.py:405-436 (execute method: post-stage hooks for spec verification)
- What: After stage completion: emit spec, enrich with property tests, verify against spec with SpecVerifier
- Engine coverage checked: no spec verification gates in MasterOrchestrator; builders have their own spec/verifier → different
- Gap severity: missing (spec-verification gating is builders-specific)
- Action: port as optional stage handler (make verification pluggable)
- Effort: M
- Product-agnostic?: Stronghold-owned (skip — spec verification is builders feature, not general orchestration)

