# S17 Mining Report: MCP/Skills/Memory/Webhooks/Gate/Mason/Traces/Schedules Routes

**Slice**: S17 — Remaining routes in Stronghold API (MCP server management, skill install/review, memory HTTP surface, webhook receivers, gate endpoint, scheduling, tracing, marketplace)

**Status**: In progress...

## Findings

### /home/user/stronghold/src/stronghold/api/routes/gate_endpoint.py:17-157
- What: POST /v1/stronghold/gate endpoint for input sanitization + LLM improvement with optional clarifying questions. Supports "best_effort", "persistent", "supervised" modes. Uses container.gate for sanitization, container.warden for re-scan, container.llm for improvement. Tracks security strikes.
- Engine coverage checked: hive-conductor/backend/routes/*, maistro-server/src/maistro_server/api/* → missing
- Gap severity: missing
- Action: new endpoint
- Effort: M
- Product-agnostic?: Stronghold-owned (skip)

### /home/user/stronghold/src/stronghold/api/routes/marketplace.py:31-489
- What: Three-endpoint marketplace pipeline: (1) GET /v1/stronghold/marketplace/skills (browse ClawHub + Claude Code Plugins), (2) GET /v1/stronghold/marketplace/agents (browse GitAgent repos), (3) POST /v1/stronghold/marketplace/scan (SSRF-protected fetch + pattern security_scan + Warden AI), (4) POST /v1/stronghold/marketplace/fix (auto-repair via fix_content, track 3x failures for auto-delist), (5) POST /v1/stronghold/marketplace/import (admin only, import at trust tier t2/t4). Uses PostgreSQL for delist tracking (fallback in-memory).
- Engine coverage checked: hive-conductor/backend/routes/*, maistro-server/src/maistro_server/api/* → missing
- Gap severity: missing
- Action: new endpoint family
- Effort: L
- Product-agnostic?: Stronghold-owned (skip)

### /home/user/stronghold/src/stronghold/api/routes/mcp.py:20-363
- What: MCP server management API. Endpoints: (1) GET /v1/stronghold/mcp/catalog (list built-in catalog), (2) GET /v1/stronghold/mcp/registries/search (search Smithery/Official/Glama with optional Warden scan), (3) GET /v1/stronghold/mcp/servers (list deployed/registered servers with live pod status from K8s), (4) POST /v1/stronghold/mcp/servers (deploy from catalog, custom image, or GitHub repo; validates name/image/port/tier), (5) POST /v1/stronghold/mcp/servers/{name}/stop (scale to 0), (6) POST /v1/stronghold/mcp/servers/{name}/start (scale to 1), (7) DELETE /v1/stronghold/mcp/servers/{name} (remove entirely). CSRF protection on mutations. Uses container.mcp_registry and container.mcp_deployer.
- Engine coverage checked: hive-conductor/backend/routes/mcp.py → partial
- Gap severity: weaker
- Action: enhance (merge catalog/registry search, add K8s deployment, add start/stop, add image validation + tier support)
- Effort: M
- Product-agnostic?: Stronghold-owned (skip)

### /home/user/stronghold/src/stronghold/api/routes/mason.py:25-464
- What: Mason agent orchestration + GitHub webhook receiver. Endpoints: (1) POST /v1/stronghold/mason/assign (enqueue issue, dispatch background Frank planner + Mason builder), (2) POST /v1/stronghold/mason/review-pr (emit reactor event for review_and_improve), (3) GET /v1/stronghold/mason/queue (list queued issues), (4) GET /v1/stronghold/mason/status (execution status), (5) GET /v1/stronghold/mason/issues (fetch open issues from GitHub with 15min/1min cache), (6) GET /v1/stronghold/mason/scan (scan codebase for good-first-issues), (7) POST /v1/stronghold/mason/scan/create (bulk create GitHub issues), (8) POST /v1/stronghold/webhooks/github (GitHub webhook receiver: issues assigned/labeled, PRs opened, PR reviews, issue comments). Webhook verification via HMAC-SHA256.
- Engine coverage checked: maistro-server/src/maistro_server/api/webhooks.py → partial
- Gap severity: partial
- Action: merge (partial parity on /webhooks/github; new endpoints: mason queue/status/assignment API + codebase scanning + GitHub cache)
- Effort: L (webhook merge) + M (new mason routes)
- Product-agnostic?: Stronghold-owned (skip)

### /home/user/stronghold/src/stronghold/api/routes/skills.py:14-432
- What: Skill lifecycle management with LLM forge. Endpoints: (1) GET /v1/stronghold/skills (list org-scoped skills), (2) POST /v1/stronghold/skills/forge (LLM-generated SKILL.md with Warden scan + trust tier t3 + parsing validation, admin only), (3) GET /v1/stronghold/skills/{name} (get full skill definition), (4) PUT /v1/stronghold/skills/{name} (update skill, admin only), (5) DELETE /v1/stronghold/skills/{name} (delete, admin only), (6) POST /v1/stronghold/skills/validate (validate SKILL.md content, return parsed fields + errors), (7) POST /v1/stronghold/skills/test (test skill execution with Warden pre/post scans, admin only). Uses CSRF protection, org-scoped registry.
- Engine coverage checked: hive-conductor/backend/routes/skills.py → stubbed
- Gap severity: weaker
- Action: enhance (add org-scoped listing, LLM forge with validation + Warden, add validate endpoint, enhance test with Warden scans, add proper trust tiers)
- Effort: M
- Product-agnostic?: Stronghold-owned (skip)

