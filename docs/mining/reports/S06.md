# Mining Report S06: Stronghold Tools

**Slice**: S06 (stronghold tools)  
**Source**: `/home/user/stronghold/src/stronghold/tools/`  
**Engine Target**: `/home/user/maistro-engine/packages/maistro-core/src/maistro/tools/`  
**Scope**: Product-agnostic only; Stronghold-owned (multi-tenant, bot auth) marked SKIP  
**Date**: 2026-07-04

---

## File-by-File Analysis

### 1. `/stronghold/src/stronghold/tools/__init__.py:1` (empty)
- **What**: Empty module init
- **Engine coverage**: Already has `/maistro/tools/__init__.py`
- **Gap severity**: None
- **Action**: Skip
- **Effort**: N/A
- **Product-agnostic?**: Yes

---

### 2. `/stronghold/src/stronghold/tools/catalog.py:1-111` / Tool Catalog with Multi-Tenant Cascade
- **What**: Registry with multi-tenant scope cascade (builtin → tenant → user), version metadata, plugin discovery via entry-points
- **Engine coverage**: Engine has `/maistro/tools/__init__.py` and protocol-based approach; no multi-tenant catalog in core
- **Gap severity**: SKIP (multi-tenant is Stronghold-owned per ADR-019/035)
- **Action**: Mark SKIP — ADR-019 delegates tenant_id/user_id scoping to Stronghold. Maistro-core uses soft scope axes (global→org→team→user→agent→session), not hard tenant boundaries.
- **Effort**: N/A (product-specific)
- **Product-agnostic?**: Stronghold-owned (skip)

---

### 3. `/stronghold/src/stronghold/tools/decorator.py:1-71` / @tool Decorator
- **What**: Function decorator registers tools to `_REGISTERED_TOOLS` global list, introspects signature, builds JSON-schema parameters. Captures docstring + version + required_permissions.
- **Engine coverage**: Engine has `/maistro/tools/result.py` and other tool infra, but no decorator. Different approach (explicit registration via registry).
- **Gap severity**: SKIP (decorator is glue for Stronghold's `CatalogEntry`-based system)
- **Action**: Mark SKIP — tightly coupled to `catalog.py` and multi-tenant scoping. Engine uses explicit registration model (protocol-driven DI), not decorators.
- **Effort**: N/A (Stronghold-specific)
- **Product-agnostic?**: Stronghold-owned (skip)

---

### 4. `/stronghold/src/stronghold/tools/executor.py:1-231` / Tool Dispatcher + SSRF Protection
- **What**: `ToolDispatcher` routes tool calls to registry executors, fallback to HTTP endpoint POST. **Critical security**: SSRF blocklist (RFC1918, loopback, IPv6 private, dangerous schemes), DNS rebinding protection via `_resolve_blocks_private()` (lines 138-168).
- **Engine coverage**: Engine has `/maistro/tools/sandbox/server.py` for HTTP execution; missing SSRF guards and DNS rebinding checks.
- **Gap severity**: HIGH (security gap — engine tools make HTTP requests without SSRF protection)
- **Action**: Port SSRF blocklist + DNS rebinding logic to engine. Engine should guard all HTTP tool endpoints against internal-IP access.
- **Effort**: Medium (copy SSRF logic ~95 lines, integrate into engine's HTTP handler)
- **Product-agnostic?**: Yes (SSRF protection is universal)

---

### 5. `/stronghold/src/stronghold/tools/file_ops.py:1-121` / File Operations with Path Sandboxing
- **What**: Sandboxed file ops (read, write, list, mkdir, exists). **Critical security** (lines 64-73): Path traversal prevention using `Path.is_relative_to()`, symlink escape detection via `resolve(strict=True)`, null-byte handling, OS error catching. Workspace-relative paths only.
- **Engine coverage**: Engine has `/maistro/tools/sandbox/workspace.py` and file handling; path sandboxing logic not visible in quick scan.
- **Gap severity**: HIGH (if engine's file_ops lacks symlink+traversal guards, engine is vulnerable)
- **Action**: Verify engine's file_ops has equivalent symlink/traversal protection. If missing, port Stronghold's `Path.is_relative_to()` + `resolve(strict=True)` pattern. This is a critical security baseline.
- **Effort**: Low (already implemented in Stronghold; verify + port if needed)
- **Product-agnostic?**: Yes (path sandboxing is universal)

---

### 6. `/stronghold/src/stronghold/tools/github.py:1-565` / GitHub API Client
- **What**: REST API wrapper for GitHub ops (issues, branches, PRs, reviews, labels). **Stronghold-specific**: Bot identity registry (lines 94-115: gatekeeper, mason, quartermaster, archie apps with IDs/keys), GitHub App JWT auth (lines 118-177). **Reusable**: All HTTP methods (lines 226-564: list_issues, create_pr, submit_review, etc.) are product-agnostic.
- **Engine coverage**: Engine has `/maistro/tools/git/github.py` — need to compare.
- **Gap severity**: MEDIUM (Stronghold-specific bot logic; engine likely has its own auth model)
- **Action**: Compare engine's github.py. Borrow API method implementations if engine is missing them (e.g., submit_review, merge_pr). Skip bot registry — that's Stronghold multi-agent orchestration.
- **Effort**: Low-Medium (review engine's gap; port ~3-5 methods if needed)
- **Product-agnostic?**: Partial (API methods yes; bot registry no)

---

### 7. `/stronghold/src/stronghold/tools/legacy.py:1` / Legacy Wrapper
- **What**: Empty placeholder for Conductor migration
- **Engine coverage**: N/A
- **Gap severity**: None
- **Action**: Skip
- **Effort**: N/A
- **Product-agnostic?**: Yes

---

### 8. `/stronghold/src/stronghold/tools/registry.py:1-104` / Tool Registry
- **What**: In-memory registry with pluggable executor callbacks. Methods: `register()`, `get()`, `get_executor()`, `list_all()`, `list_for_task()`, `to_openai_schemas()`. Supports group-based filtering (task_type matching).
- **Engine coverage**: Engine has `/maistro/tools/__init__.py` but need to verify if it has equivalent registry. Core approach is protocol-driven DI.
- **Gap severity**: MEDIUM (engine may lack a registry pattern; currently might rely on hardcoded tool imports)
- **Action**: Review engine's tool registration model. If engine uses hardcoded tool lists, consider adopting a registry pattern (or bridge). This registry is product-agnostic and reusable.
- **Effort**: Medium (integrate into engine's tool subsystem; may require refactoring)
- **Product-agnostic?**: Yes

---

### 9. `/stronghold/src/stronghold/tools/scanner.py:1-355` / Codebase Scanner for Good-First-Issues
- **What**: Detects good-first-issue opportunities in source code (missing fakes, untested modules, missing docstrings, TODOs, sidebar inconsistencies). Converts to GitHub issue payloads.
- **Engine coverage**: Not present in engine (Stronghold-specific developer-facing tool)
- **Gap severity**: None (not a runtime tool; Stronghold contributor aid)
- **Action**: Skip — pure Stronghold maintenance/contributor tool, not product-agnostic
- **Effort**: N/A (Stronghold-specific)
- **Product-agnostic?**: Stronghold-owned (skip)

---

### 10. `/stronghold/src/stronghold/tools/shell_exec.py:1-225` / Shell Execution with Allowlist + Metachar Guard
- **What**: Sandboxed shell command execution. **Critical security** (lines 155-177): Allowlist of safe prefixes (pytest, ruff, mypy, bandit, git, pip, ls, cat, grep, find). Blocks metacharacters (`;|&\`$(){}<>` + newlines) to prevent injection. Blocked patterns (rm -rf /, dd if=, mkfs, curl|sh, wget|sh). Timeout 120s.
- **Engine coverage**: Engine likely has shell sandbox; need to verify metachar guards.
- **Gap severity**: HIGH if engine lacks metachar injection guards
- **Action**: Verify engine's shell_exec has equivalent guards. If missing, port allowlist + metachar block (lines 98-131). This prevents `echo hi; rm -rf /` style injections.
- **Effort**: Low (allowlist + regex check ~30 lines)
- **Product-agnostic?**: Yes (shell security is universal)

---

### 11. `/stronghold/src/stronghold/tools/workspace.py:1-232` / Git Worktree Manager for Mason
- **What**: Creates/manages isolated git worktrees for code changes (clone, branch, worktree add/remove). Git identity config (user.email, user.name as "Mason"). Clone + fetch + push workflow. GITHUB_TOKEN env var for auth.
- **Engine coverage**: Engine has `/maistro/tools/git/shadow.py` and `/maistro/tools/git/server.py`; need to compare worktree logic.
- **Gap severity**: MEDIUM (engine may lack worktree orchestration; important for agent code-gen)
- **Action**: Compare engine's git module. If engine lacks worktree support, consider porting `_create()`, `_commit()`, `_push()`, `_cleanup()` methods. This is product-agnostic (used by any agent that needs isolated code changes).
- **Effort**: Medium (port ~80 lines of git orchestration)
- **Product-agnostic?**: Yes (worktree management is universal)

---

## Summary Table

| File | Category | Gap Severity | Action | Agnostic? |
|------|----------|--------------|--------|-----------|
| `__init__.py` | Init | None | Skip | Yes |
| `catalog.py` | Registry | SKIP | Skip (multi-tenant) | Stronghold |
| `decorator.py` | Registration | SKIP | Skip (Stronghold glue) | Stronghold |
| **`executor.py`** | **Tool dispatch** | **HIGH** | **Port SSRF guards** | **Yes** |
| **`file_ops.py`** | **Path sandbox** | **HIGH** | **Verify/port symlink guards** | **Yes** |
| `github.py` | API client | MEDIUM | Review + borrow methods | Partial |
| `legacy.py` | Placeholder | None | Skip | Yes |
| `registry.py` | Registry | MEDIUM | Adopt registry pattern | Yes |
| `scanner.py` | Contributor tool | None | Skip (Stronghold-specific) | Stronghold |
| **`shell_exec.py`** | **Shell sandbox** | **HIGH** | **Verify/port metachar guards** | **Yes** |
| **`workspace.py`** | **Git workflow** | **MEDIUM** | **Port worktree methods** | **Yes** |

---

## Critical Gaps (HIGH severity, action required)

1. **SSRF Protection** (`executor.py:68-135`): Engine tools lack SSRF blocklist and DNS rebinding checks. Required for secure HTTP tool invocation.
2. **Path Traversal/Symlink Escapes** (`file_ops.py:64-73`): Verify engine's file_ops uses `Path.is_relative_to()` + symlink detection. Critical baseline.
3. **Shell Injection Prevention** (`shell_exec.py:155-177`): Verify engine's shell guards block metacharacters. Required for safe arbitrary command execution.

---

## Recommendations

- **Immediate**: Audit engine's `file_ops` and `shell_exec` for security baseline gaps.
- **Near-term**: Port SSRF protection to engine's HTTP tool handler.
- **Medium-term**: Consider adopting Stronghold's registry pattern (cleaner than hardcoded tools).
- **Workstream**: Git worktree support (if engine lacks it) unblocks agent code-gen isolation.

---

*Report generated: 2026-07-04T00:00:00Z*
