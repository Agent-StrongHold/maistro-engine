# S09 Mining Report: Stronghold Persistence + Sessions + Quota

**Status:** In Progress
**Date Started:** 2026-07-04
**Slice:** S09 (product-agnostic only; tag Stronghold-owned items)

---

### /home/user/stronghold/src/stronghold/persistence/pg_agents.py:1-182
- What: Agent registry with CRUD; upsert includes org_id filtering and 8 review/config columns (preamble, config, ai_reviewed, ai_review_clean, ai_review_flags, admin_reviewed, admin_reviewed_by, user_reviewed)
- Engine coverage checked: /home/user/maistro-engine/packages/maistro-core/src/maistro/persistence/pg_agents.py → partial
- Gap severity: partial
- Action: Review & align: engine has AgentIdentity (no org_id, no review cols), stronger JSON handling (_decode_json), souls() method; stronghold has org_id tenant filtering + review audit trail
- Effort: M
- Product-agnostic?: Stronghold-owned (skip) — org_id filtering + review columns are Stronghold-specific; AgentIdentity is core; engine stronger on JSON handling

### /home/user/stronghold/src/stronghold/persistence/pg_audit.py:1-102
- What: Immutable audit log with org_id + user_id + agent_id filtering; records boundary, verdict, tool_name
- Engine coverage checked: /home/user/maistro-engine/packages/maistro-core/src/maistro/persistence/pg_audit.py → parity (org_id omitted from engine, getattr fallbacks added)
- Gap severity: partial
- Action: Review query params — stronghold includes org_id in INSERT and filter columns; engine omits org_id as product-agnostic (per ADR-019/035)
- Effort: S
- Product-agnostic?: Stronghold-owned (skip) — org_id filtering is multi-tenant; core has user/agent only

### /home/user/stronghold/src/stronghold/persistence/pg_learnings.py:1-220
- What: Learning store with org_id + team_id isolation; dedup/find_relevant/mark_outcome/check_auto_promotions/get_promoted all org-scoped; INSERT includes both; _row_to_learning reads both
- Engine coverage checked: /home/user/maistro-engine/packages/maistro-core/src/maistro/persistence/pg_learnings.py → partial
- Gap severity: partial
- Action: All query-level org_id filtering is Stronghold-only per ADR-019 (org is soft scope, not hard tenancy). Core correctly omits org_id/team_id from INSERT. Verify Learning dataclass matches scope defaults ("organization" vs "agent").
- Effort: S
- Product-agnostic?: Stronghold-owned (skip) — org_id + team_id are Stronghold multi-tenant; core product-agnostic design verified

