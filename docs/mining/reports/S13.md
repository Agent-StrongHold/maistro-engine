# Mining Report S13: Stronghold Protocols

Status: COMPLETE

## Summary

Mined all 21 protocol files from Stronghold (`src/stronghold/protocols/`) against maistro-core engine (`packages/maistro-core/src/maistro/protocols/`). 

**Key findings:**
- **Parity (15 protocols)**: `AgentStore`, `AuthProvider`, `IntentClassifier`, `EmbeddingClient`, `FeedbackExtractor`, `ViolationStore`, `LLMClient`, `PromptManager`, `QuotaTracker`, `ModelRouter`, `SkillLoader`, `SkillForge`, `SkillMarketplace`, `SpecStore`, `SpecVerifier`, all tool/tracing protocols
- **Partial/Delta (5 protocols)**: `LearningStore` (engine adds `list_all()`), `EpisodicStore` (param scope differences per ADR-068), `OutcomeStore` (engine adds `project_id` param), `SecretBackend` (docstring gaps on Cedar/policy), `ToolPlugin` (auth type Any vs AuthContext)
- **Engine-only (1 protocol)**: `ContextAssemblyPolicy` (ADR-091 layered context assembly)
- **Missing from engine (4 protocols)**: `DataStore` (low-level abstraction), `RateLimiter` (rate limiting), `McpDeployerClient` (K8s deployer), `VaultClient` (per-user vault)
- **Stronghold-owned, skip (3 entries)**: `AgentPodDiscovery`, `McpDeployerClient`, `VaultClient`/`VaultSecret`

**Action summary**: 9 items need merge/port (mostly docstring enrichment or signature unification); 7 can port to engine if needed; 3 are Stronghold-only K8s/multi-tenant features.

---

### /home/user/stronghold/src/stronghold/protocols/agent_pod.py:47
- What: `AgentPodDiscovery` protocol — find/register/unregister pods; `AgentPodInfo` dataclass (ip, generation, pod_name)
- Engine coverage checked: protocols/ → missing
- Gap severity: missing
- Action: skip (Stronghold-owned, K8s-specific per-tenant pod discovery)
- Effort: N/A
- Product-agnostic?: Stronghold-owned (skip)

### /home/user/stronghold/src/stronghold/protocols/agents.py:12
- What: `AgentStore` protocol — CRUD for runtime agent management; create/get/list_all/update/delete/export_gitagent/import_gitagent
- Engine coverage checked: protocols/agents.py:12 → present, identical
- Gap severity: parity
- Action: parity (no delta)
- Effort: N/A
- Product-agnostic?: yes

### /home/user/stronghold/src/stronghold/protocols/auth.py:12
- What: `AuthProvider` protocol — authenticate(authorization, headers) → AuthContext
- Engine coverage checked: protocols/auth.py:9 → present, nearly identical (returns Any vs AuthContext)
- Gap severity: parity (minor typing difference)
- Action: parity (no functional delta)
- Effort: N/A
- Product-agnostic?: yes

### /home/user/stronghold/src/stronghold/protocols/classifier.py:13
- What: `IntentClassifier` protocol — classify(messages, task_types, explicit_priority) + detect_multi_intent(user_text, task_types)
- Engine coverage checked: protocols/classifier.py:13 → present, identical
- Gap severity: parity
- Action: parity (no delta)
- Effort: N/A
- Product-agnostic?: yes

### /home/user/stronghold/src/stronghold/protocols/data.py:9
- What: `DataStore` protocol — execute(query, *args), execute_one(query, *args), insert(table, data)
- Engine coverage checked: protocols/ → missing
- Gap severity: missing
- Action: new (product-agnostic data abstraction; engine uses concrete persistence layer, no protocol seam)
- Effort: M
- Product-agnostic?: yes

### /home/user/stronghold/src/stronghold/protocols/embeddings.py:13
- What: `EmbeddingClient` protocol — dimension property; embed(text) + embed_batch(texts)
- Engine coverage checked: protocols/embeddings.py:13 → present, identical
- Gap severity: parity
- Action: parity (no delta)
- Effort: N/A
- Product-agnostic?: yes

### /home/user/stronghold/src/stronghold/protocols/feedback.py:22
- What: `FeedbackExtractor` protocol — extract_learnings(result) → list[Learning]
- Engine coverage checked: protocols/feedback.py:22 → present, identical
- Gap severity: parity
- Action: parity (no delta)
- Effort: N/A
- Product-agnostic?: yes

### /home/user/stronghold/src/stronghold/protocols/feedback.py:31
- What: `ViolationStore` protocol — record_finding/record_review/get_metrics/get_top_violations
- Engine coverage checked: protocols/feedback.py:31 → present, identical
- Gap severity: parity
- Action: parity (no delta)
- Effort: N/A
- Product-agnostic?: yes

### /home/user/stronghold/src/stronghold/protocols/llm.py:12
- What: `LLMClient` protocol — complete(messages, model, tools, ...) + stream(messages, model, **kwargs)
- Engine coverage checked: protocols/llm.py:12 → present, identical
- Gap severity: parity
- Action: parity (no delta)
- Effort: N/A
- Product-agnostic?: yes

### /home/user/stronghold/src/stronghold/protocols/mcp.py:32
- What: `McpDeployerClient` protocol — deploy_tool_mcp(tool_name, image) + stop_tool_mcp(deployment_name) + health()
- Engine coverage checked: protocols/ → missing
- Gap severity: missing
- Action: skip (Stronghold-owned; K8s MCP deployer sidecar, issue #381)
- Effort: N/A
- Product-agnostic?: Stronghold-owned (skip)

### /home/user/stronghold/src/stronghold/protocols/memory.py:13
- What: `LearningStore` protocol — store/find_relevant/mark_used/mark_outcome/check_auto_promotions/get_promoted
- Engine coverage checked: protocols/memory.py:13 → present with extensions
- Gap severity: partial (engine adds `list_all()` method at line 53)
- Action: merge (engine has superset; Stronghold can adopt list_all if needed for admin)
- Effort: S
- Product-agnostic?: yes

### /home/user/stronghold/src/stronghold/protocols/memory.py:58
- What: `LearningExtractor` protocol — extract_corrections/extract_positive_patterns
- Engine coverage checked: protocols/memory.py:59 → present, identical
- Gap severity: parity
- Action: parity (no delta)
- Effort: N/A
- Product-agnostic?: yes

### /home/user/stronghold/src/stronghold/protocols/memory.py:79
- What: `EpisodicStore` protocol — store/retrieve (with agent_id, user_id, team, task_type)/reinforce
- Engine coverage checked: protocols/memory.py:80 → present with deltas
- Gap severity: partial (engine: retrieve has team_id+org_id vs. Stronghold's team+task_type; engine adds list_by_scope())
- Action: merge (scope params differ per ADR-068; engine has richer retrieve signature)
- Effort: M
- Product-agnostic?: yes

### /home/user/stronghold/src/stronghold/protocols/memory.py:105
- What: `OutcomeStore` protocol — record/get_task_completion_rate/get_experience_context/get_usage_breakdown/get_daily_timeseries/list_outcomes
- Engine coverage checked: protocols/memory.py:119 → present with delta
- Gap severity: partial (engine: get_experience_context adds org_id, project_id params at line 139-140)
- Action: merge (engine signature is superset)
- Effort: S
- Product-agnostic?: yes

### /home/user/stronghold/src/stronghold/protocols/memory.py:158
- What: `SkillMutationStore` protocol — record/list_mutations
- Engine coverage checked: protocols/memory.py:174 → present, identical
- Gap severity: parity
- Action: parity (no delta)
- Effort: N/A
- Product-agnostic?: yes

### /home/user/stronghold/src/stronghold/protocols/memory.py:171
- What: `RCAExtractor` protocol — extract_rca(user_text, tool_history) → Learning | None
- Engine coverage checked: protocols/memory.py:187 → present, identical
- Gap severity: parity
- Action: parity (no delta)
- Effort: N/A
- Product-agnostic?: yes

### /home/user/stronghold/src/stronghold/protocols/memory.py:184
- What: `SessionStore` protocol — get_history/append_messages/delete_session
- Engine coverage checked: protocols/memory.py:200 → present, identical (docstring differs: Stronghold adds "Org-scoped via session ID format")
- Gap severity: parity
- Action: parity (no functional delta)
- Effort: N/A
- Product-agnostic?: yes

### /home/user/stronghold/src/stronghold/protocols/memory.py:210
- What: `AuditLog` protocol — log/get_entries
- Engine coverage checked: protocols/memory.py:262 → present, identical
- Gap severity: parity
- Action: parity (no delta)
- Effort: N/A
- Product-agnostic?: yes

### Engine-only: /home/user/maistro-engine/packages/maistro-core/src/maistro/protocols/memory.py:226
- What: `ContextAssemblyPolicy` protocol — layer0/layer1/layer2/layer3/layer4/assemble (ADR-091)
- Engine coverage checked: protocols/memory.py:226 → engine-only, Stronghold missing
- Gap severity: missing
- Action: port (product-agnostic context assembly per ADR-091; Stronghold may need for layered context)
- Effort: M
- Product-agnostic?: yes

### /home/user/stronghold/src/stronghold/protocols/prompts.py:9
- What: `PromptManager` protocol — get(name, label) + get_with_config(name, label) + upsert(name, content, config, label)
- Engine coverage checked: protocols/prompts.py:9 → present, identical (docstring: "PostgreSQL-backed" vs "versioned")
- Gap severity: parity
- Action: parity (no functional delta)
- Effort: N/A
- Product-agnostic?: yes

### /home/user/stronghold/src/stronghold/protocols/quota.py:9
- What: `QuotaTracker` protocol — record_usage/get_usage_pct/get_all_usage
- Engine coverage checked: protocols/quota.py:9 → present, identical
- Gap severity: parity
- Action: parity (no delta)
- Effort: N/A
- Product-agnostic?: yes

### /home/user/stronghold/src/stronghold/protocols/rate_limit.py:9
- What: `RateLimiter` protocol — check(key) → (bool, dict), record(key)
- Engine coverage checked: protocols/ → missing
- Gap severity: missing
- Action: new (product-agnostic rate-limiting abstraction; not in engine yet)
- Effort: S
- Product-agnostic?: yes

### /home/user/stronghold/src/stronghold/protocols/router.py:14
- What: `ModelRouter` protocol — select(intent, models, providers, routing_config) → ModelSelection
- Engine coverage checked: protocols/router.py:14 → present, identical
- Gap severity: parity
- Action: parity (no delta)
- Effort: N/A
- Product-agnostic?: yes

### /home/user/stronghold/src/stronghold/protocols/secrets.py:33
- What: `SecretResult` dataclass — value: str, version: str | None
- Engine coverage checked: protocols/secrets.py:28 → present, identical
- Gap severity: parity (docstring differs: engine is terse, Stronghold more detailed)
- Action: parity (no functional delta)
- Effort: N/A
- Product-agnostic?: yes

### /home/user/stronghold/src/stronghold/protocols/secrets.py:47
- What: `SecretBackend` protocol — get_secret(ref), watch_changes(ref), close()
- Engine coverage checked: protocols/secrets.py:36 → present with docstring gaps
- Gap severity: partial (Stronghold's docstring mentions Cedar PDP #700, PermissionError, tenant-isolation policy-gating; engine version shorter)
- Action: merge (signature identical; update docstrings in engine to include tenant isolation details per ADR-K8S-003)
- Effort: S
- Product-agnostic?: yes

### /home/user/stronghold/src/stronghold/protocols/skills.py:14
- What: `SkillLoader` protocol — load_all() + merge_into_tools()
- Engine coverage checked: protocols/skills.py:14 → present, identical
- Gap severity: parity
- Action: parity (no delta)
- Effort: N/A
- Product-agnostic?: yes

### /home/user/stronghold/src/stronghold/protocols/skills.py:30
- What: `SkillForge` protocol — forge(request) + mutate(skill_name, learning)
- Engine coverage checked: protocols/skills.py:30 → present, identical
- Gap severity: parity
- Action: parity (no delta)
- Effort: N/A
- Product-agnostic?: yes

### /home/user/stronghold/src/stronghold/protocols/skills.py:50
- What: `SkillMarketplace` protocol — search(query) + install(url, trust_tier) + uninstall(name)
- Engine coverage checked: protocols/skills.py:50 → present, identical
- Gap severity: parity
- Action: parity (no delta)
- Effort: N/A
- Product-agnostic?: yes

### /home/user/stronghold/src/stronghold/protocols/spec.py:16
- What: `SpecStore` protocol — save/get/list_active
- Engine coverage checked: protocols/spec.py:16 → present, identical
- Gap severity: parity
- Action: parity (no delta)
- Effort: N/A
- Product-agnostic?: yes

### /home/user/stronghold/src/stronghold/protocols/spec.py:33
- What: `SpecVerifier` protocol — verify(spec, stage, result) → VerificationResult
- Engine coverage checked: protocols/spec.py:33 → present, identical
- Gap severity: parity
- Action: parity (no delta)
- Effort: N/A
- Product-agnostic?: yes

### /home/user/stronghold/src/stronghold/protocols/tools.py:13
- What: `ToolExecutor` protocol — name property + execute(arguments)
- Engine coverage checked: protocols/tools.py:12 → present, identical
- Gap severity: parity
- Action: parity (no delta)
- Effort: N/A
- Product-agnostic?: yes

### /home/user/stronghold/src/stronghold/protocols/tools.py:27
- What: `ToolRegistry` protocol — get_definitions/get_executor/register
- Engine coverage checked: protocols/tools.py:26 → present, identical
- Gap severity: parity
- Action: parity (no delta)
- Effort: N/A
- Product-agnostic?: yes

### /home/user/stronghold/src/stronghold/protocols/tools.py:49
- What: `ToolPlugin` protocol — name property + get_definition() + execute(arguments, auth)
- Engine coverage checked: protocols/tools.py:48 → present with delta
- Gap severity: partial (engine: execute auth param is Any vs Stronghold's AuthContext; docstring shorter)
- Action: merge (signature delta; consider unifying auth type or documenting why Any)
- Effort: S
- Product-agnostic?: yes

### /home/user/stronghold/src/stronghold/protocols/tracing.py:12
- What: `Span` protocol — __enter__/__exit__ + set_input/set_output/set_usage
- Engine coverage checked: protocols/tracing.py:12 → present, identical
- Gap severity: parity
- Action: parity (no delta)
- Effort: N/A
- Product-agnostic?: yes

### /home/user/stronghold/src/stronghold/protocols/tracing.py:33
- What: `Trace` protocol — trace_id property + span/score/update/end methods
- Engine coverage checked: protocols/tracing.py:33 → present, identical
- Gap severity: parity
- Action: parity (no delta)
- Effort: N/A
- Product-agnostic?: yes

### /home/user/stronghold/src/stronghold/protocols/tracing.py:46
- What: `TracingBackend` protocol — create_trace(user_id, session_id, name, metadata)
- Engine coverage checked: protocols/tracing.py:46 → present, identical
- Gap severity: parity
- Action: parity (no delta)
- Effort: N/A
- Product-agnostic?: yes

### /home/user/stronghold/src/stronghold/protocols/vault.py:18
- What: `VaultSecret` dataclass — value: str, service: str, key: str, version: int | None
- Engine coverage checked: protocols/ → missing
- Gap severity: missing
- Action: skip (Stronghold-owned; per-user credential vault for tool executors, ADR-K8S-018, org_id-scoped)
- Effort: N/A
- Product-agnostic?: Stronghold-owned (skip)

### /home/user/stronghold/src/stronghold/protocols/vault.py:28
- What: `VaultClient` protocol — get_user_secret/put_user_secret/delete_user_secret/list_user_services/revoke_user/close
- Engine coverage checked: protocols/ → missing
- Gap severity: missing
- Action: skip (Stronghold-owned; per-user vault per ADR-K8S-018; org_id, user_id scoped)
- Effort: N/A
- Product-agnostic?: Stronghold-owned (skip)

---

