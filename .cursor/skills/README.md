# Project skills

Add repeatable workflows here as subfolders, each with a `SKILL.md` (YAML frontmatter + body). Example:

```
.cursor/skills/my-workflow/
└── SKILL.md
```

Personal skills can live in `~/.cursor/skills/`; commit project-specific skills here so the team shares them.
