# imported rules

Cursor stores **remote / GitHub-imported** project rules here. Paths mirror the source repo.

Do **not** hand-edit files in this directory — edits may be overwritten on the next sync. Add team-authored rules under `.cursor/rules/core/`, `.cursor/rules/packages/`, or `.cursor/rules/manual/` instead.
