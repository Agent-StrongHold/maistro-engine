"""Vendored third-party benchmark graders and corpora.

Everything under here is upstream code, reproduced by a script and pinned by
content hash — see each subpackage's NOTICE and scripts/vendor_*.py. It is
excluded from this repo's lint and type gates because it must stay
byte-comparable to upstream; reformatting it would break that comparison and
silently make the grader ours rather than the published one.
"""
