name: validator.check
role: validator
description: Verify extracted output builds, imports cleanly, and passes tests
prompt_name: validator.check
result_schema: schemas.ValidatorOutput
min_tier: 2
max_tier: 3
temperature: 0.2
max_tokens: 2048
